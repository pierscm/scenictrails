<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobileapps.lewisu.cassiepierson.scenictrails">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PreviousRunActivity" />
        <activity android:name=".NewRunActivity" />
        <activity android:name=".FinalizeNewTrailActivity" />
        <activity android:name=".CreateRunActivity" />
        <activity android:name=".TrailViewActivity" />
        <activity android:name=".BeginRun" />
        <activity android:name=".ReviewTrailActivity"></activity>
    </application>

</manifest>